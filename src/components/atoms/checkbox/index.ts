import CustomCheckbox from './CustomCheckbox';
import withMotion from '../withMotion';

const MotionCheckbox = withMotion(CustomCheckbox);

export { CustomCheckbox, MotionCheckbox };
export default CustomCheckbox;
