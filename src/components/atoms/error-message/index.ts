import ErrorMessage from './SimpleErrorMessage';
import withMotion from '../withMotion';

const MotionErrorMessage = withMotion(ErrorMessage);

export { ErrorMessage, MotionErrorMessage };
export default ErrorMessage;
